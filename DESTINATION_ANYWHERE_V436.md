# Any chosen destination — v436

The 45 m endpoint snap gate and path-only network prevented real-life quests from starting. Main Quests now always produce a saved plan for valid selected coordinates. Public walking roads connect mapped paths; bounded disconnected-component search fills gaps explicitly. If map data is absent, partial, too large, unavailable or outside the bounded search area, the same selected endpoints still produce direction guidance. Provider requests have a 12-second total budget; offline requests skip providers. Cancellation still discards the request.

Mapped sections retain existing source-node/way/access evidence. A new version-2 route stores mapped parts and explicit guidance parts separately; guidance appears dashed and is described as direction rather than a known public path. Existing version-1 saves continue to validate. The player chooses an accessible way on the ground. Existing honor-based Finish my walk and Complete & claim work regardless of GPS/checkpoint/track compliance, retaining atomic once-only rewards and saved encounters.

Validation: nine new anywhere cases cover distant endpoints, road joins, disconnected paths, absent/private/partial maps, identical/short/far selections, provider failures and timeouts, cancellation, altered guidance and altered mapped evidence. Existing strict mapped-evidence tests remain separately exercised through planMappedDestinationRoute (11), provider recovery (5), state/transaction/reload (13), UI/lifecycle/pins (8), and map-load overlay (2). Both changed JS modules and the UI stylesheet are pinned in the document and all three service-worker lists; global cache/build advances to destination-anywhere-v436-20260921. Legacy updater already includes both modules. No recognition, billing, art or Side Quest changes.

Browser/release evidence lives in /root/burbz-destination-v436-evidence. Phone-shaped Chromium with disposable saves tests actual installed v435c→v436 update, exact cached bytes, native Preview/Begin through provider outage, offline active-quest reload, native Finish/claim and once-only completion persistence. These are browser tests, not physical outdoor GPS measurements.
