'use strict';
const fs=require('node:fs'),vm=require('node:vm'),path=require('node:path'),assert=require('node:assert/strict'),core=require('../building_work_core.js');
let now=20000,click;const button={hidden:true,replaceChildren(){},append(){},addEventListener(_,fn){click=fn;},remove(){this.removed=true;}},hint={textContent:''};
const ledger={key:'village:7:cabin',label:'Cabin',startMs:1000,endMs:101000},target={id:'cabin',startMs:1000};let saves=0,fail=false;
class V{set(x,y,z){Object.assign(this,{x,y,z});return this;}}
const ctx={console,Date:{now:()=>now},THREE:{Vector3:V,Box3:class{setFromObject(){this.min={x:1.4,z:-2.6};this.max={x:6.6,z:2.6};return this;}}},BurbzBuildingWorkCore:core,document:{createElement:()=>button}};vm.createContext(ctx);vm.runInContext(fs.readFileSync(path.join(__dirname,'../building_work.js'),'utf8'),ctx);
const s={options:{seed:7,work:{describe:()=>({...ledger}),assist:()=>{if(fail)return{ok:false,error:'Disk full'};const next=core.assist(ledger,now);if(!next)return{ok:false};Object.assign(ledger,next);saves++;return{ok:true};}}},root:{append(){},querySelector:selector=>selector==='.vw-hint'?hint:null},abort:{signal:{}},player:{x:0,y:0,z:0,mode:'walk'},source:{buildings:[{userData:{buildingId:'cabin',construction:target},getWorldPosition:p=>p.set(4,0,0)}]},world:{height:()=>0,allowed:()=>true}};
const work=ctx.BurbzBuildingWork.attach(s);let frame=0;const update=()=>work.update(frame+=.3);
update();assert(!button.hidden);click();click();assert.equal(saves,0);assert.equal(work.diagnostics().taps,2);s.player.x=-20;update();assert(button.hidden);s.player.x=0;update();click();assert.equal(work.diagnostics().taps,1);assert.equal(saves,0);console.log('PASS Leaving the actual site cancels incomplete help');
s.player.mode='fly';update();assert(button.hidden);assert.equal(work.key('KeyF'),false);s.player.mode='walk';s.world.height=()=>null;update();assert(button.hidden);s.world.height=()=>0;s.world.allowed=()=>false;update();assert(button.hidden);s.world.allowed=()=>true;update();assert(!button.hidden);console.log('PASS Flight, unknown terrain and blocked approaches cannot help');
fail=true;for(let i=0;i<3;i++)click();assert.equal(saves,0);assert.equal(ledger.endMs,101000);assert.match(hint.textContent,/Disk full/);fail=false;for(let i=0;i<3;i++)assert(work.key('KeyF'));assert.equal(saves,1);assert.equal(ledger.endMs,76000);assert(button.hidden);assert.equal(work.key('KeyF'),false);update();assert(button.hidden);console.log('PASS Three nearby taps/keys commit exactly one quarter reduction, with retry after failed saving');
work.dispose();assert(button.removed);assert.equal(work.diagnostics().sites,0);console.log('PASS Clean frame-owned teardown removes the interaction');
