/* Disposable old-installed -> new-installed -> offline play proof. */
const http=require('node:http'),fs=require('node:fs'),path=require('node:path'),assert=require('node:assert/strict');
const {chromium}=require(process.env.PLAYWRIGHT_MODULE||'/root/src/gstack/node_modules/playwright');
const current=path.resolve(__dirname,'..'),baseline=process.env.BASELINE_ROOT||'/root/burbz-v380-baseline/public/burbz',out=process.env.EVIDENCE_DIR||'/root/burbz-map-buildings-v381-evidence/pwa';fs.mkdirSync(out,{recursive:true});let version='old';const missing=[];
const server=http.createServer((req,res)=>{const rel=decodeURIComponent(new URL(req.url,'http://localhost').pathname).replace(/^\/burbz\//,'')||'index.html',root=version==='old'?baseline:current;let file=path.resolve(root,rel);if(!file.startsWith(root+'/')){res.writeHead(403);return res.end();}try{let data=fs.readFileSync(file);if(data.length<200&&data.toString().startsWith('version https://git-lfs.github.com/spec/'))data=fs.readFileSync(path.join(current,rel));if(rel==='index.html')data=Buffer.from(data.toString().replace('\ninit();','\nwindow.__testEval=code=>eval(code);\ninit();'));res.setHeader('Content-Type',({'.html':'text/html','.js':'text/javascript','.json':'application/json','.css':'text/css','.webp':'image/webp','.png':'image/png','.svg':'image/svg+xml','.mp3':'audio/mpeg'})[path.extname(file)]||'application/octet-stream');res.setHeader('Cache-Control','no-cache');res.end(data);}catch{missing.push(rel);res.writeHead(404);res.end();}});
(async()=>{await new Promise(r=>server.listen(8885,'127.0.0.1',r));const browser=await chromium.launch({headless:true,executablePath:process.env.CHROME_PATH||'/root/.cache/ms-playwright/chromium-1228/chrome-linux64/chrome',args:['--no-sandbox','--use-angle=swiftshader']});let report;
try{const context=await browser.newContext({viewport:{width:390,height:844}});await context.route('**/*',r=>{const u=r.request().url();if(u.startsWith('https://api.gbif.org/v1/occurrence/search?'))return r.fulfill({contentType:'application/json',body:fs.readFileSync(path.join(__dirname,'fixtures/area_birds_v377',new URL(u).searchParams.has('month')?'seasonal-records.json':'annual-records.json'),'utf8')});return u.startsWith('http://localhost:8885/')?r.continue():r.abort();});const page=await context.newPage(),errors=[];page.on('pageerror',e=>errors.push(e.message));const run=fn=>page.evaluate(fn=>__testEval('('+fn+')()'),fn.toString());await page.goto('http://localhost:8885/burbz/',{waitUntil:'domcontentloaded'});await page.waitForFunction(()=>navigator.serviceWorker.controller&&localStorage.getItem('burbz_state'),null,{timeout:120000});console.log('Old PWA installed');
await run(()=>{if(merlinTutActive)endMerlinTutorial(false);localStorage.setItem(BURBZ_INTRO_SEEN_KEY,'1');localStorage.setItem(BURBZ_TUTORIAL_CHAPTERS_KEY,JSON.stringify(MERLIN_TUTORIAL_CHAPTERS.map(c=>c.id)));gameState.player.level=20;gameState.player.coins=123456;gameState.player.branches=4321;gameState.fullCardProof='old-save-kept';gameState.playerHome=BurbzPlayerHomeCore.initial(true);gameState.playerHome.tier=1;BurbzPlayerHome.close('fixture');gameState.academyBuildings.kitchen={built:true,builtAt:'fixture'};gameState.academyBuildings.tavern={built:true,builtAt:'fixture'};localStorage.setItem('burbz_area_birds_open','1');const bird=createBirdEntry('Great Spotted Woodpecker','Dendrocopos major',.9);bird.id='pwa-card';gameState.flock=[bird];gameState.inventory.gear={thorn_talons:2};equipItemOnBird(bird.id,'weapon','thorn_talons');const e=ensureEmpireState();e.pendingLiberation={seed:103,name:'Third Village',lat:53.353,lon:-1.786};e.liberationVictories['103']={seed:103,wonAt:new Date().toISOString()};e.townCharters=[];e.cityCharters=[];e.villages={};for(const seed of [101,102]){e.villages[seed]={seed,name:'Willow '+seed,lat:53.349+(seed-101)*.002,lon:-1.786,claimedAt:new Date().toISOString()};}showEmpirePage('realm',{silent:true});ensureWalkingQuestState().active={id:'v381-installed-walk',name:'Saved walk',birdsCaptured:[],startedAt:1,distanceWalkedM:200,route:[[53.349,-1.786],[53.35,-1.786],[53.352,-1.786]],routeCertification:{status:'certified'},checkpoints:[{kind:'flag',lat:53.349,lon:-1.786,reached:false},{kind:'chest',lat:53.35,lon:-1.786,reached:false},{kind:'finish',lat:53.352,lon:-1.786,reached:false}],chestsOpened:0};saveState();});
await page.reload({waitUntil:'domcontentloaded'});await page.waitForFunction(()=>window.__testEval?.('gameState.fullCardProof')==='old-save-kept');
version='new';await page.evaluate(()=>navigator.serviceWorker.getRegistration().then(r=>r.update()));await page.waitForFunction(()=>window.__testEval?.('BURBZ_BUILD')==='map-trails-v381-20260909',null,{timeout:120000});console.log('New PWA activated');
assert.equal(await run(()=>gameState.player.coins),123456);assert.equal(await run(()=>birdLoadout('pwa-card').weapon),'thorn_talons');assert.equal(await run(()=>gameState.fullCardProof),'old-save-kept');
const files=['map_trail_core.js','quest_core.js','geographic_details_scene.js','geographic_places.js'];
for(const name of files){const cached=await page.evaluate(async name=>{const cache=await caches.open((await caches.keys()).find(k=>k.endsWith('map-trails-v381-20260909'))),r=await cache.match(name+'?v=map-trails-v381-20260909');if(!r?.ok)throw Error('Missing '+name);return Array.from(new Uint8Array(await r.arrayBuffer()));},name);assert.deepEqual(Buffer.from(cached),fs.readFileSync(path.join(current,name)));}
assert.equal(await run(()=>currentScreen),'scan');assert.equal(await run(()=>activeWalkingQuest().id),'v381-installed-walk');const originalRoute=await run(()=>JSON.stringify(activeWalkingQuest().route));
await context.setOffline(true);await page.reload({waitUntil:'domcontentloaded'});await page.waitForFunction(()=>window.__testEval?.('BURBZ_BUILD')==='map-trails-v381-20260909');assert.equal(await run(()=>JSON.stringify(activeWalkingQuest().route)),originalRoute);assert.equal(await run(()=>gameState.playerHome.tier),1);
const chest=await run(()=>{const q=activeWalkingQuest(),cp=q.checkpoints[1];cp.loot=BurbzQuestCore.normaliseChestLoot(null,'v381-installed');liveMapHasPrecisePosition=true;const near=BurbzMapTrailCore.destination(cp,0,11);liveMapLastPosition={...near,accuracy:8,at:Date.now()};const coins=gameState.player.coins;questOnPositionFix(near.lat,near.lon,8);return {coins,award:cp.loot.coins,reached:q.checkpoints[1].reached,flag:q.checkpoints[0].reached};});assert(chest.reached);assert(!chest.flag);assert.equal(await run(()=>gameState.player.coins),chest.coins+chest.award);
await page.reload({waitUntil:'domcontentloaded'});await page.waitForFunction(()=>window.__testEval);assert(await run(()=>activeWalkingQuest().checkpoints[1].reached));assert.equal(await run(()=>JSON.stringify(activeWalkingQuest().route)),originalRoute);assert.equal(await run(()=>birdLoadout('pwa-card').weapon),'thorn_talons');assert.equal(await run(()=>currentScreen),'scan');assert.deepEqual(errors,[]);
report={oldInstalled:true,automaticUpgrade:true,exactCachedModuleBytes:true,activeQuestAndCoordinatesRetained:true,offlineChestAt11m:true,earlierFlagStillRequired:true,claimReload:true,homeAndEquipmentRetained:true,errors,missing:[...new Set(missing)]};fs.writeFileSync(path.join(out,'results.json'),JSON.stringify(report,null,2));console.log(JSON.stringify(report));
}finally{await browser.close();server.close();}})().catch(e=>{console.error(e);server.close();process.exitCode=1;});
