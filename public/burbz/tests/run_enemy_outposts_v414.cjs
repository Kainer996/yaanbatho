'use strict';
const fs=require('node:fs'),path=require('node:path'),assert=require('node:assert/strict'),{chromium}=require(process.env.PLAYWRIGHT_MODULE||'/home/yaan/.cache/codex-runtimes/codex-primary-runtime/dependencies/node/node_modules/playwright'),F=require('./connected_world_fixture_v386.cjs');
const root=path.resolve(__dirname,'..'),out=process.env.EVIDENCE_DIR||'/tmp/burbz-outpost-proof',report={served:{},missing:[],checks:[],errors:[],limits:'Disposable save; deterministic DEM/vector fixture; real renderer and native attack input. Guarded debug travel/aim, no injected enemies or damage. Desktop Chromium responsive simulation, not phone hardware.'};fs.mkdirSync(out,{recursive:true});const server=F.createServer({root,port:8991,report});let browser,page;const run=code=>page.evaluate(code=>__testEval(code),code),read=()=>page.evaluate(()=>__burbzVillageWalkDebug.state()),place=p=>page.evaluate(p=>__burbzVillageWalkDebug.place(p),p),pass=name=>{report.checks.push(name);console.log('PASS',name);};
(async()=>{try{await server.listen();browser=await chromium.launch({headless:true,executablePath:'/usr/bin/chromium',args:['--no-sandbox','--use-angle=swiftshader','--enable-unsafe-swiftshader']});const context=await browser.newContext({viewport:{width:1280,height:800},serviceWorkers:'block'});await F.routeMap(context,report);await context.route('**/burbz/**',async route=>{const rel=decodeURIComponent(new URL(route.request().url()).pathname).replace(/^\/burbz\//,'');if(!/^(assets|bird-art-cache|icons|lib)\//.test(rel)||rel.includes('..'))return route.fallback();for(const base of [root,'/tmp/burbz-v394-assets','/home/yaan/Documents/Codex/2026-09-06/burbz-visual-polish/work/world-sky-v401/public/burbz','/home/yaan/Documents/Codex/2026-09-06/burbz-visual-polish/work/four-bird-manga-v411/public/burbz']){const file=path.join(base,rel);if(fs.existsSync(file)&&fs.statSync(file).size>200)return route.fulfill({path:file});}return route.fallback();});page=await context.newPage();page.on('pageerror',e=>{report.errors.push(e.message);console.log('PAGE ERROR',e.message);});await page.goto(server.url,{waitUntil:'domcontentloaded'});await page.waitForFunction(()=>window.__testEval&&__testEval('currentScreen')==='scan',null,{timeout:60000});
await run(`if(merlinTutActive)endMerlinTutorial(false);const e=ensureEmpireState();e.villages={};e.townCharters=[];e.cityCharters=[];e.mergeChartersVersion=1;const v=e.villages[101]={seed:101,name:'Alder Hollow',lat:54.45,lon:-2.65,claimedAt:new Date().toISOString(),liberatedAt:new Date().toISOString()};const eco=ensureVillageEconomy(v);eco.buildings={cabin:1,well:1};eco.population=4;eco.ruins=[];eco.constructions=[];gameState.player.level=1;gameState.inventory.equipment['@player']={weapon:'willow_wand',spell:'ember_wisp',armour:'aegis_of_dawn'};gameState.enemyOutposts=BurbzEnemyOutpostsCore.empty();saveState();openEmpireVillage(101);`);
await page.locator('#villageWalkBtn:not([disabled])').waitFor({timeout:60000});await page.locator('#villageWalkBtn').click();await page.waitForFunction(()=>window.__burbzVillageWalkDebug?.state().ready||window.__burbzVillageWalkDebug?.state().failed,null,{timeout:90000});assert(!(await read()).failed);pass('Actual retained walking scene loads with no camp in the safe settlement');
const targets=await run('BurbzEnemyOutpostsCore.nearby(BurbzGeographicWorldCore.unproject({lat:54.45,lon:-2.65},{x:2700,y:0,z:0})).map(c=>({...c,...BurbzGeographicWorldCore.project({lat:54.45,lon:-2.65},c)})).filter(c=>Math.hypot(c.x,c.z)>300)');report.targets=targets;
await page.locator('.cw-wings').click();await page.locator('.vw-look').focus();await page.keyboard.down('Space');await page.waitForTimeout(3000);await page.keyboard.up('Space');
let found=null;for(const target of targets.slice(0,5)){for(let n=0;n<140;n++){const d=await read(),local=require('../geographic_world_core.js').project(d.continuity.origin,target),dx=local.x-d.player.x,dz=local.z-d.player.z,dist=Math.hypot(dx,dz);if(dist<25)break;const step=Math.min(24,dist);const p={...d.player,x:d.player.x+dx/dist*step,z:d.player.z+dz/dist*step,y:90,mode:'fly'};if(!await place(p)){await page.waitForTimeout(400);continue;}await page.waitForFunction(()=>__burbzVillageWalkDebug.state().continuity.visibleCoverage,null,{timeout:30000});await page.waitForTimeout(80);}await page.waitForTimeout(2400);console.log('SCAN',JSON.stringify({outposts:(await read()).combat.outposts,player:(await read()).player,target}));if((await read()).combat.outposts.visible.some(v=>v.id===target.id)){found={...target,...await run('gameState.enemyOutposts.camps['+JSON.stringify(target.id)+']')};break;}}
assert(found,'Suitable natural outpost found');report.camp=found;pass('A deterministic real camp is discovered through loaded wilderness travel');
let d=await read();const campLocal=require('../geographic_world_core.js').project(d.continuity.origin,found);const landing=await page.evaluate(c=>{const w=__burbzVillageWalkDebug.world();for(const z of [8,10,12,0,-8])for(const x of [0,8,-8,12,-12]){const p={x:c.x+x,z:c.z+z},h=w.height(p.x,p.z);if(!w.allowed(p.x,p.z))continue;let clear=true;for(let y=h+.65;y<95;y+=.7)if(!w.combatAllowed3(p.x,y,p.z)){clear=false;break;}if(clear)return{...p,y:90};}return null;},campLocal);assert(landing,'Clear vertical landing beside camp');assert(await place({...d.player,...landing,mode:'fly'}));await page.locator('.vw-look').focus();await page.keyboard.down('ShiftLeft');for(let n=0;n<80;n++){if(await page.evaluate(()=>{const p=__burbzVillageWalkDebug.state().player;return p.y-__burbzVillageWalkDebug.world().height(p.x,p.z)<1.6;}))break;await page.waitForTimeout(500);}await page.keyboard.up('ShiftLeft');await page.locator('.cw-wings').click();await page.waitForFunction(()=>__burbzVillageWalkDebug.state().player.mode==='walk');await page.waitForFunction(()=>__burbzVillageWalkDebug.state().combat.actors.some(a=>a.campId),null,{timeout:30000});report.before=await read();await page.screenshot({path:path.join(out,'hostile-camp-desktop.png')});pass('Landing activates the real three-guard combat roster and camp cover');
const initialXP=await run('gameState.player.xp'),campId=found.id;await page.locator('.wc-spell').click();
for(let shot=0;shot<45;shot++){if(await run('gameState.enemyOutposts.camps['+JSON.stringify(campId)+'].liberated'))break;d=await read();if(d.combat.hero.hp<=0)throw Error('Player died before camp liberation');if(!d.combat.ready){await page.waitForTimeout(300);shot--;continue;}const a=d.combat.actors.find(a=>a.campId===campId&&a.fighter.hp>0);if(!a){await page.waitForTimeout(500);continue;}const dx=a.position.x-d.player.x,dz=a.position.z-d.player.z;assert(await place({...d.player,yaw:Math.atan2(-dx,-dz),pitch:Math.atan2(a.position.y-d.player.y-1.55,Math.hypot(dx,dz))}));await page.locator('.vw-look').focus();await page.keyboard.press('KeyQ');await page.waitForTimeout(700);console.log('SHOT',shot,await run('gameState.enemyOutposts.camps['+JSON.stringify(campId)+'].hp'));}
assert(await run('gameState.enemyOutposts.camps['+JSON.stringify(campId)+'].liberated'));pass('Native Fireball attacks defeat actual defenders and atomically liberate the camp');report.after=await read();report.saved=await run('structuredClone(gameState.enemyOutposts)');assert.equal(await run('BurbzEnemyOutpostsCore.damage(structuredClone(gameState.enemyOutposts),[],Date.now()).xp'),0);await page.screenshot({path:path.join(out,'friendly-outpost-desktop.png')});
for(const viewport of [{width:390,height:844},{width:844,height:390}]){await page.setViewportSize(viewport);await page.waitForTimeout(400);const b=await page.locator('.eo-status').boundingBox();assert(b&&b.x>=0&&b.x+b.width<=viewport.width&&b.y>=0&&b.y+b.height<viewport.height);await page.screenshot({path:path.join(out,'friendly-'+viewport.width+'.png')});}pass('Camp status and collect control fit portrait and landscape alongside existing controls');
await page.locator('.vw-exit').click();assert.equal(await page.locator('.eo-status').count(),0);const saved=await run('structuredClone(gameState.enemyOutposts)');await page.reload({waitUntil:'domcontentloaded'});await page.waitForFunction(()=>window.__testEval&&__testEval('currentScreen')==='scan');assert.deepEqual(await run('gameState.enemyOutposts.camps['+JSON.stringify(campId)+'].hp'),[0,0,0]);assert(await run('gameState.enemyOutposts.camps['+JSON.stringify(campId)+'].liberated'));pass('Exit disposes camp resources and reload preserves ownership and all defeated guards');assert.deepEqual(report.errors,[]);report.complete=true;
}catch(e){console.error(e);report.failure=e.stack;process.exitCode=1;try{report.state=await read();await page.screenshot({path:path.join(out,'failure.png')});}catch{}}finally{fs.writeFileSync(path.join(out,'results.json'),JSON.stringify(report,null,2));await browser?.close();server.server.close();}})();
