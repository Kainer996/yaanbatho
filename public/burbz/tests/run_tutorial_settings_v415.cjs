'use strict';
const fs=require('node:fs'),path=require('node:path'),assert=require('node:assert/strict'),{chromium}=require('/home/yaan/.cache/codex-runtimes/codex-primary-runtime/dependencies/node/node_modules/playwright'),F=require('./connected_world_fixture_v386.cjs');
const root=path.resolve(__dirname,'..'),out='/tmp/burbz-tutorial-settings-v415',report={served:{},missing:[],checks:[],errors:[]};fs.mkdirSync(out,{recursive:true});const fixture=F.createServer({root,port:8996,report,seed:false});let browser,page;const run=code=>page.evaluate(code=>__testEval(code),code),pass=x=>{report.checks.push(x);console.log('PASS',x);};
(async()=>{try{await fixture.listen();browser=await chromium.launch({headless:true,executablePath:'/usr/bin/chromium',args:['--no-sandbox','--use-angle=swiftshader','--enable-unsafe-swiftshader']});for(const size of [{width:390,height:844},{width:844,height:390},{width:1280,height:800}]){const context=await browser.newContext({viewport:size,hasTouch:size.width<1000,serviceWorkers:'block'});await F.routeMap(context,report);await context.route('**/burbz/**',async route=>{const rel=decodeURIComponent(new URL(route.request().url()).pathname).replace(/^\/burbz\//,'');if(!/^(assets|bird-art-cache|icons|lib)\//.test(rel)||rel.includes('..'))return route.fallback();for(const base of ['/home/yaan/Documents/Codex/2026-09-06/burbz-visual-polish/work/new-game-trailer-v415/public/burbz', '/home/yaan/Documents/Codex/2026-09-06/burbz-visual-polish/work/gemini-offline-v410/work/runtime-assets', '/home/yaan/Documents/Codex/2026-09-06/burbz-visual-polish/work/companion-release-v363/public/burbz', '/home/yaan/Documents/Codex/2026-09-06/burbz-visual-polish/work/world-sky-v401/public/burbz', '/home/yaan/Documents/Codex/2026-09-06/burbz-visual-polish/work/v362-update-http-cache/burbz']){const f=path.join(base,rel);if(fs.existsSync(f)&&fs.statSync(f).size>200)return route.fulfill({path:f});}return route.fallback();});page=await context.newPage();page.on('pageerror',e=>report.errors.push(e.message));await page.goto(fixture.url,{waitUntil:'domcontentloaded'});await page.locator('#introSkipBtn').click();await page.locator('#merlinTutorialOverlay.show').waitFor();
for(const [id,mode] of [['lesson-0','dialogue'],['lesson-3','spotlight'],['alderwing-arrival-v395','free action']]){
await run(`merlinTutShowStep(merlinTutSequence.findIndex(i=>MERLIN_TUTORIAL_STEPS[i].id===${JSON.stringify(id)}))`);await page.waitForTimeout(450);
const before=await run('JSON.stringify({home:gameState.playerHome,inventory:gameState.inventory,player:gameState.player,seen:merlinChaptersSeen()})');
assert.equal(await page.locator('#settingsBtn').evaluate(e=>!!e.closest('[inert]')),false);const box=await page.locator('#settingsBtn').boundingBox();assert(box.width>=44&&box.height>=44&&box.x>=0&&box.y>=0&&box.x+box.width<=size.width&&box.y+box.height<=size.height);
await page.locator('#settingsBtn').click();await page.locator('#settingsModal.show').waitFor();assert.equal(await run('merlinTutActive'),false);assert.equal(await run('readMerlinTutorialState().stepId'),id);
page.once('dialog',d=>d.dismiss());await page.locator('#startNewGameBtn').click();assert.equal(await run('JSON.stringify({home:gameState.playerHome,inventory:gameState.inventory,player:gameState.player,seen:merlinChaptersSeen()})'),before);assert.equal(await page.locator('#settingsModal').isVisible(),true);
if(mode==='spotlight')await page.keyboard.press('Escape');else if(mode==='free action')await page.evaluate(()=>history.back());else await page.locator('#settingsCloseBtn').click();
await page.locator('#merlinTutorialOverlay.show').waitFor();assert.equal(await run('merlinTutCurrentStep().id'),id);assert.equal(await run('JSON.stringify({home:gameState.playerHome,inventory:gameState.inventory,player:gameState.player,seen:merlinChaptersSeen()})'),before);
await page.locator('#settingsBtn').focus();await page.keyboard.press('Enter');await page.locator('#settingsModal.show').waitFor();await page.locator('#settingsCloseBtn').click();assert.equal(await run('merlinTutCurrentStep().id'),id);pass(size.width+'×'+size.height+' '+mode+': native Settings, cancelled reset, close/back and keyboard resume exact lesson without progress/reward change');
}
await page.screenshot({path:path.join(out,'free-'+size.width+'.png')});await context.close();}
assert.deepEqual(report.errors,[]);report.complete=true;
}catch(e){report.failure=e.stack;console.error(e);process.exitCode=1;try{await page.screenshot({path:path.join(out,'failure.png')});}catch{}}finally{fs.writeFileSync(path.join(out,'results.json'),JSON.stringify(report,null,2));await browser?.close();fixture.server.close();}})();
