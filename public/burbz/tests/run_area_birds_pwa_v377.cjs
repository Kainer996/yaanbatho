/* Disposable old-installed -> new-installed -> offline play proof. */
const http=require('node:http'),fs=require('node:fs'),path=require('node:path'),assert=require('node:assert/strict');
const {chromium}=require(process.env.PLAYWRIGHT_MODULE||'/root/src/gstack/node_modules/playwright');
const current=path.resolve(__dirname,'..'),baseline=process.env.BASELINE_ROOT||'/root/burbz-v376-baseline/public/burbz',out=process.env.EVIDENCE_DIR||'/root/burbz-area-birds-v377-evidence/pwa';fs.mkdirSync(out,{recursive:true});let version='old';const missing=[];
const server=http.createServer((req,res)=>{const rel=decodeURIComponent(new URL(req.url,'http://localhost').pathname).replace(/^\/burbz\//,'')||'index.html',root=version==='old'?baseline:current;let file=path.resolve(root,rel);if(!file.startsWith(root+'/')){res.writeHead(403);return res.end();}try{let data=fs.readFileSync(file);if(data.length<200&&data.toString().startsWith('version https://git-lfs.github.com/spec/'))data=fs.readFileSync(path.join(current,rel));if(rel==='index.html')data=Buffer.from(data.toString().replace('\ninit();','\nwindow.__testEval=code=>eval(code);\ninit();'));res.setHeader('Content-Type',({'.html':'text/html','.js':'text/javascript','.json':'application/json','.css':'text/css','.webp':'image/webp','.png':'image/png','.svg':'image/svg+xml','.mp3':'audio/mpeg'})[path.extname(file)]||'application/octet-stream');res.setHeader('Cache-Control','no-cache');res.end(data);}catch{missing.push(rel);res.writeHead(404);res.end();}});
(async()=>{await new Promise(r=>server.listen(8882,'127.0.0.1',r));const browser=await chromium.launch({headless:true,executablePath:process.env.CHROME_PATH||'/root/.cache/ms-playwright/chromium-1228/chrome-linux64/chrome',args:['--no-sandbox','--use-angle=swiftshader']});let report;
try{const context=await browser.newContext({viewport:{width:390,height:844}});await context.route('**/*',r=>{const u=r.request().url();if(u.startsWith('https://api.gbif.org/v1/occurrence/search?'))return r.fulfill({contentType:'application/json',body:fs.readFileSync(path.join(__dirname,'fixtures/area_birds_v377',new URL(u).searchParams.has('month')?'seasonal-records.json':'annual-records.json'),'utf8')});return u.startsWith('http://localhost:8882/')?r.continue():r.abort();});const page=await context.newPage(),errors=[];page.on('pageerror',e=>errors.push(e.message));const run=fn=>page.evaluate(fn=>__testEval('('+fn+')()'),fn.toString());await page.goto('http://localhost:8882/burbz/',{waitUntil:'domcontentloaded'});await page.waitForFunction(()=>navigator.serviceWorker.controller&&localStorage.getItem('burbz_state'),null,{timeout:120000});console.log('Old PWA installed');
await run(()=>{if(merlinTutActive)endMerlinTutorial(false);localStorage.setItem(BURBZ_INTRO_SEEN_KEY,'1');localStorage.setItem(BURBZ_TUTORIAL_CHAPTERS_KEY,JSON.stringify(MERLIN_TUTORIAL_CHAPTERS.map(c=>c.id)));gameState.player.level=20;gameState.player.coins=123456;gameState.player.branches=4321;gameState.fullCardProof='old-save-kept';localStorage.setItem('burbz_area_birds_open','1');const bird=createBirdEntry('Great Spotted Woodpecker','Dendrocopos major',.9);bird.id='pwa-card';gameState.flock=[bird];gameState.inventory.gear={thorn_talons:2};equipItemOnBird(bird.id,'weapon','thorn_talons');const e=ensureEmpireState();e.pendingLiberation={seed:103,name:'Third Village',lat:53.353,lon:-1.786};e.liberationVictories['103']={seed:103,wonAt:new Date().toISOString()};e.townCharters=[];e.cityCharters=[];e.villages={};for(const seed of [101,102]){e.villages[seed]={seed,name:'Willow '+seed,lat:53.349+(seed-101)*.002,lon:-1.786,claimedAt:new Date().toISOString()};}showEmpirePage('realm',{silent:true});saveState();});
await page.reload({waitUntil:'domcontentloaded'});await page.waitForFunction(()=>window.__testEval?.('gameState.fullCardProof')==='old-save-kept');
version='new';await page.evaluate(()=>navigator.serviceWorker.getRegistration().then(r=>r.update()));await page.waitForFunction(()=>window.__testEval?.('BURBZ_BUILD')==='area-birds-v377-20260909',null,{timeout:120000});console.log('New PWA activated');
assert.equal(await run(()=>gameState.player.coins),123456);assert.equal(await run(()=>birdLoadout('pwa-card').weapon),'thorn_talons');assert.equal(await run(()=>gameState.fullCardProof),'old-save-kept');
for(const name of ['area_birds_core.js','area_birds.js','area_birds_taxonomy.js','area_birds.css','geographic_map_3d.js'])assert(await page.evaluate(async name=>{const c=await caches.open((await caches.keys()).find(k=>k.endsWith('area-birds-v377-20260909')));return (await c.match(name+'?v=area-birds-v377-20260909'))?.ok;},name));
await run(()=>{liveMapHasPrecisePosition=true;liveMapLastPosition={lat:53.349,lon:-1.786,at:Date.now()};switchScreen('map');});
await page.locator('#mapAreaBirdsToggle').click();await page.waitForFunction(()=>BurbzAreaBirds.state()?.evidence);const names=await page.locator('.area-bird-card h2').allTextContents();assert(names.length>9);assert.equal(await page.locator('#mapAreaBirdsList').count(),0);
const kept=await run(()=>JSON.stringify({flock:gameState.flock,birdex:gameState.birdex,inventory:gameState.inventory,coins:gameState.player.coins,walkingQuests:gameState.walkingQuests}));
await context.setOffline(true);await page.reload({waitUntil:'domcontentloaded'});await page.waitForFunction(()=>window.__testEval?.('BURBZ_BUILD')==='area-birds-v377-20260909');
await run(()=>{liveMapHasPrecisePosition=true;liveMapLastPosition={lat:53.349,lon:-1.786,at:Date.now()};switchScreen('map');});await page.locator('#mapAreaBirdsToggle').click();await page.waitForFunction(()=>BurbzAreaBirds.state()?.evidence);assert.deepEqual(await page.locator('.area-bird-card h2').allTextContents(),names);
await page.locator('#areaBirdsSort').selectOption('rare');await page.locator('#areaBirdsSearch').fill('Robin');assert.equal(await page.locator('.area-bird-card').count(),1);await page.locator('.area-bird-open').click();await page.locator('#mapBirdPreview .map-bird-preview-close').click();await page.locator('#areaBirdsBack').click();assert.equal(await run(()=>currentScreen),'map');assert.equal(await run(()=>JSON.stringify({flock:gameState.flock,birdex:gameState.birdex,inventory:gameState.inventory,coins:gameState.player.coins,walkingQuests:gameState.walkingQuests})),kept);
assert.equal(await run(()=>gameState.fullCardProof),'old-save-kept');assert.equal(await run(()=>birdLoadout('pwa-card').weapon),'thorn_talons');

await run(()=>{empireLedgerOnlyMode=true;returnToLiberatedVillage();});await page.locator('#villageClaimBar button').waitFor({state:'visible'});assert.match(await page.locator('#villageClaimBar').innerText(),/BUILD LIBERATION BIRDHOUSE/);await page.locator('#villageClaimBar button').click();assert.equal(await run(()=>empireVillages().length),3);await page.reload({waitUntil:'domcontentloaded'});await page.waitForFunction(()=>window.__testEval);assert.equal(await run(()=>empireVillages().length),3);assert.equal(await run(()=>ensureEmpireState().townCharters.length),0);assert.equal(await run(()=>gameState.fullCardProof),'old-save-kept');assert.equal(await run(()=>birdLoadout('pwa-card').weapon),'thorn_talons');await page.screenshot({path:path.join(out,'offline-third-village.png')});assert.deepEqual(errors,[]);

report={oldInstalled:true,automaticUpgrade:true,oldSaveAndEquipmentPreserved:true,areaModulesCached:true,areaBirdsCacheAndListWorkOffline:true,oldDropdownPreferenceIgnored:true,discoveryAndEconomyUnchanged:true,thirdVictoryReturnAndClaimWorkOffline:true,claimedVillageReloadRetained:true,errors,missing:[...new Set(missing)]};fs.writeFileSync(path.join(out,'results.json'),JSON.stringify(report,null,2));console.log(JSON.stringify(report));
}finally{await browser.close();server.close();}})().catch(e=>{console.error(e);server.close();process.exitCode=1;});
