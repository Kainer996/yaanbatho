# Photo framing and verification v393

Prepared locally on `codex/burbz-photo-accuracy-v393`, based on released `8d18aae7`. Not published. Yaan reports clear raven captures returning “Bird not found”, and a zoom limit preventing framing.

The native cropper used to shrink every photo to 3072 pixels before framing, then stop at 8×. A browser regression reproduces a 6000-pixel original becoming 3072 pixels. Framing now retains original source pixels up to 24 MP (8192-pixel longest-side guard), uses a separate small display preview, and only downsizes the chosen crop for upload. Minimum crop size is 48 source pixels; zoom cannot create missing detail. The source bitmap is closed on exit/replacement. A 6000×4000 camera fixture now gives a real 200×200 upload at 20× zoom. Touch pinch, anchor math, bounds and square layouts are exercised.

After Identify, the photo stays in the same full-screen sheet. The bar is indeterminate because the model supplies no actual completion percentage. Preparation, checking and outcome are explicit; landscape uses the photo beside the controls. Close cancels the client request and prevents late rewards. Failures stay visible, reframe/retake remain available, and retry uses the exact earlier JPEG. Success returns through the existing discovery/save funnel.

The recognizer keeps confidence >=0.90, margin >=0.20, clear diagnostic evidence and original-pixel quality checks. Instructions now distinguish a dark bird from an uninformative silhouette and allow genuinely diagnostic partial views. They specifically caution against judging black birds by colour or apparent size alone. The second reading sees the original and a source-pixel crop, without the first answer. Both must independently pass the gate and agree on scientific identity; confidence is their lower score, not a calibrated probability. Disagreement abstains. This is an extra check by the same model, not independent expert validation or a guarantee of accuracy.

Both calls share 35 seconds and at most one transient-service retry per reading. An inconclusive answer is never retried until it passes. The browser retains its total 45-second deadline. The new `photo-evidence-v393` marker and `verified:true` are required by the frontend, so a stale backend cannot silently bypass the second check. Quota errors explain that the service limit, not image quality, prevented analysis.

Reference for corvid field features: [Cornell Lab identification](https://www.allaboutbirds.org/guide/Common_Raven/id). Fixtures and licences are in `tests/fixtures/photo-v350/README.md`.

## Evidence and remaining checks

Local evidence: `/root/burbz-photo-accuracy-evidence/`. Real model trials accepted the perched Common Raven and Carrion Crow, and the flight-raven confusion fixture changed from a confidently wrong Crow to abstention when the two readings disagreed. This is a small diagnostic sample, not a field accuracy estimate. The user's actual photos and physical phone were not supplied. The recorded real-model trials predate the mechanical policy-marker promotion and clearer quota message.

Provider tests hit `GenerateRequestsPerDayPerProjectPerModel-FreeTier`, limit 20, on 2026-09-11. Real checks for the remaining Robin/Great Tit/negative controls could not complete. These quota failures are retained in `matrix.json`, `final-controls.json` and the diagnostic record; they must not be reported as successful negative recognition tests. The tests consumed the remaining shared allowance, which also affects production photo identification. Production files were not edited. Model verification needs two calls per accepted image, so the free tier permits at most ten such photos per day before retries/other usage.

Google documents the daily reset at midnight Pacific time: [rate limits](https://ai.google.dev/gemini-api/docs/rate-limits). Higher capacity requires owner billing approval; current [billing guidance](https://ai.google.dev/gemini-api/docs/billing) says a minimum $5 prepayment can be required. [Gemini 2.5 Flash pricing](https://ai.google.dev/gemini-api/docs/pricing#gemini-2.5-flash) is $0.30 per million text/image input tokens and $2.50 per million output/thinking tokens. Per-photo token use was not measured, so no per-photo price is claimed. No billing change is authorized or performed.

Local Node/Python regressions cover rejection, missing verification, model disagreement, lower confidence, unavailable verification, quota errors, shared deadlines, cancellation, original-detail framing, native touch and saved discoveries/reload. New backend tests and the large-photo browser test fail on the released baseline. The broad comparison has the same 65 existing failure IDs, with no new failures; see `pytest-comparison.json`. Final scoped counts and browser records are in the evidence README.

Before publication: finish real-model controls after capacity is available; use `scripts/verify-photo-id.py` against a staging endpoint with the nine credited fixtures (flight raven may correctly abstain, but must never become Crow). Get Yaan's publication approval, then use the existing GitHub/guarded-sync route. The photo installer must promote and verify the adapter before the shell, restoring the previous adapter on a failed proof. It now requires the verification marker and includes corvid fixtures. Verify actual public bytes, installed worker update and live outcomes. Do not directly edit `/home/ubuntu/yaanbatho/burbz`, bypass the proof, or include the unrelated `videos/friend-shaped.mp4` worktree difference.
