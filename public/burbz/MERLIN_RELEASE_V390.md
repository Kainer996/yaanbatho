# Merlin 2D flight release — v390

Integrates the separately reviewed 2D artwork/animation commit `c14383c888c4924e97974f67ef9131a2ef12b0c3` on top of the released landscape and market changes. The editable 3D Blender model is not included.

The 164-frame atlas has five 16-pose wingbeat clips, six eight-pose glides and three 12-pose pickup sequences. Painted leg extension, toe closure and retraction accompany pickup; the approach heading remains steady during grip recovery before the return turn. The original perched character and v1 artwork remain available. Artwork provenance and animation contracts are in `assets/merlin-flight/README-v2.md`.

The renderer and atlas metadata both use `merlin-flight-v390-20260910` in the app and all three service-worker installation lists. The app's explicit atlas URL and metadata URL both select `merlin-flight-v2.webp`; that image is included in all three lists and the established live updater. Integration also moves the permanent perch and care menu beside the landscape navigation rail; the original full-game runner exposed that the new rail had covered the native care tap target. Portrait is unchanged. The unchanged flight stylesheet keeps its v1 query pin. The app/cache epoch advances to v390, with historical latest-build assertions advanced independently of their original module pins.

Validation includes both existing and v2 animation models (26 checks), v1/v2 pixel checks including 164 distinct sprites and transparent cell borders, the installation contract, the existing market transaction tests and the full-game browser runner on the integrated app (146 checks passed, with no page errors). It uses disposable saves and checks Play/Rest costs and rewards, pickup, lifecycle cancellation, orientation, keyboard/touch, reduced motion and both appearance themes. The runner accepts optional local caches for unchanged sparse-checkout artwork; it never reads a private user save. The new image is approximately 2.5 MB compressed and 23.625 MiB decoded.
